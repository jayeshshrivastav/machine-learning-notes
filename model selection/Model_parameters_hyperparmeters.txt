# Model Parameters
These are the parameters of the model that can be determined by training with
training data. These can be considered as internal Parameters.
-> Weights
-> Bias

Y = w*X + b

# Hyper Parameters
Hyperparameters are parameters whose values control the learning process. 
These are adjustable parametersused to obtain an optimal model. External Parameters.
-> Learning rate.
-> Number of Epochs.

# Model Parameters
Weights -> weight decides how much influence the input will have on the output.
if the inlfuence of the column is positive weight will have positive impact.
else the influence of the column is negative weigth will have negative impact.
Y = 

Slope ->